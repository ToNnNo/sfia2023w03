<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
        {% block stylesheets %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </head>
    <body>
        <div class="container-fluid">
            <header class="pt-5">
                <h1>Formation Symfony Initiation/Approfondissement</h1>
                <hr />
            </header>
            <nav class="nav">
                <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                <a class="nav-link" href="{{ path('response_index') }}">Les types de Réponse</a>
                <a class="nav-link" href="{{ path('twig_index') }}">Twig</a>
                <a class="nav-link" href="{{ path('post_save') }}">Sauvegarde static d'un article</a>
                <a class="nav-link" href="{{ path('post_index') }}">Liste des articles</a>
                <a class="nav-link" href="{{ path('video_index') }}">Voir la vidéo</a>
                <a class="nav-link" href="{{ path('video_subscribe') }}">Prendre un abonnement</a>
                <a class="nav-link" href="{{ path('service_index') }}">Les Services</a>
                <a class="nav-link" href="{{ path('service_decode') }}">Décoder message</a>
                <a class="nav-link" href="{{ path('translation_index') }}">Les Traductions</a>

                <a class="nav-link" href="{{ path('admin_index') }}">Administration</a>

                {% if not app.user %}
                    <a class="nav-link" href="{{ path('authentication_login') }}">Connexion</a>
                {% else %}
                    <a class="nav-link disabled">Bonjour {{ app.user.userIdentifier | capitalize }}</a>
                    <a class="nav-link" href="{{ path('profile_index') }}">Mon Profil</a>
                    <a class="nav-link" href="{{ path('authentication_logout') }}">Déconnexion</a>
                {% endif %}
            </nav>
            <main>
            {% for type, messages in app.flashes %}
            <div class="alert alert-{{ type }}">
                {% for message in messages %}
                    {{ message }} <br />
                {% endfor %}
            </div>
            {% endfor %}
            {% block body %}
                <h2>{{ block('title') }}</h2>
            {% endblock %}
            </main>
            <footer>
                {% block footer %}
                <p class="text-center small">Formation Symfony - Dawan Paris - Janvier 2023</p>
                {% endblock footer %}
            </footer>
        </div>
    </body>
</html>
