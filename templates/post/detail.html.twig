{% extends 'base.html.twig' %}

{% block title %}Liste des articles{% endblock %}

{% block body %}
    <div class="float-end">
        <a href="{{ path('post_index') }}" class="btn btn-sm btn-outline-secondary">
            Retour à la liste
        </a>
    </div>
    <article>
        <h2>{{ post.title }}</h2>
        <p>
            <small><b>Auteur:</b> {{ post.author.fullname }} ({{ post.author.email }})</small>
        </p>
        <div>
            {{ post.body | raw }}
        </div>
    </article>
{% endblock %}
